The delete command, d, removes audio samples from the current session.
It may be qualified by a block address, or a comma separated pair
of block addresses, to specify which samples are to be deleted.
After the deletion, any marks that existed in the session are adjusted
appropriately.

